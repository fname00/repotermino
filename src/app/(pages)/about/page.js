export const metadata = {
  title: "Teneryfa.org.pl",
};

import AboutComponent from './aboutcomponent';  // Fix the import, capitalize

const About = () => {
  return <AboutComponent />;  // Fix the JSX component name, capitalize
};

export default About;
