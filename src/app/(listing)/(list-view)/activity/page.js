import ActivityClient from './ActivityClient'; // Import the Client Component

export const metadata = {
  title: "List || Teneryfa.org.pl",
};

export default function Page() {
  return <ActivityClient />;
}
